#include <iostream>
#include <stdlib.h>
#include <cmath>
#include <algorithm>
#define MAX_M 1000


using namespace std;

long long psum[MAX_N][MAX_N];

int main() {
	long long N, M, K;
	cin >> N >> M >> K;
	for(int i=0; i<M; i++) {
		long long x, y;
		cin >> x >> y;
	}
}
