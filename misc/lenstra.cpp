#include <iostream>

#define BND 10000

using namespace std;


// return x coordinate of 2P mod N + true
// if error, the denominator + false
pair<long long, bool> PP(long long x, long long N) {

}

// return a factor of N using Lentstra Elliptic Curve Method
long long lenstra(long long N) {
    while(true) {
        long long a = 1;    // normally (a, b) would be randomized
        long long b = 1;
        long long x = 0;    // pick P = (0,1)


    }
}

int main() {
    long long N = 448866199;
    while(N > 1) {
        lenstra(N);
    }
}